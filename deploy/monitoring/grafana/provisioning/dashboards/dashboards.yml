apiVersion: 1

providers:
  - name: DeltaDatabase
    orgId: 1
    folder: DeltaDatabase
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
