{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "user_credentials.v1",
  "title": "User Credentials Schema",
  "description": "Schema for user authentication data stored in the users database, keyed by email address",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique user identifier"
    },
    "password_hash": {
      "type": "string",
      "description": "Hashed password using a strong algorithm such as bcrypt, scrypt, Argon2, or PBKDF2"
    }
  },
  "required": ["id", "password_hash"],
  "additionalProperties": false
}
